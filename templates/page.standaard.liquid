<body class="body-colored">
    <div class="section-3">
        <div class="content-card">
            <h1 class="page-heading">{{ page.title }}</h1>
            <p class="paragraph-long">{{ page.content }}</p>
        </div>
    </div>
    
    {% include 'footer' %}
</body>

